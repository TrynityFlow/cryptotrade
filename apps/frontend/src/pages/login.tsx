import { LoginForm } from '../components';
import { LoginLayout } from '../layouts';

export const Login = () => {
  return (
    <>
      <LoginLayout>
        <LoginForm />
      </LoginLayout>
    </>
  );
};

export default Login;
